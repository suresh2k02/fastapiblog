import database
from sqlalchemy import String, Column, Integer

Base = database.Base
class Blog(Base):
    __tablename__ = 'blogs'
    id = Column(Integer, primary_key=True, index=True)
    title = Column(String)
    body = Column(String)
